import { build } from 'esbuild';
import { mkdir, writeFile } from 'node:fs/promises';
const output = await build({ entryPoints: ['src.mjs'], bundle: true, write: false, format: 'esm', minify: true, target: 'es2022' });
const js = output.outputFiles[0].text.replace(/<\/script/gi, '<\\/script');
const css = `:root{color-scheme:light dark;font-family:system-ui,sans-serif;--bg:#fff;--fg:#18202a;--muted:#566272;--line:#ccd3db}[data-theme=dark]{--bg:#15191f;--fg:#edf0f4;--muted:#b1bcc9;--line:#46515f}body{margin:0;background:var(--bg);color:var(--fg)}main{max-width:720px;margin:auto;padding:24px}h1{font-size:26px}h2{font-size:20px}p{line-height:1.7;white-space:pre-wrap;overflow-wrap:anywhere}section{border:1px solid var(--line);border-radius:14px;padding:20px;margin:20px 0}fieldset{border:0;padding:0}label{display:block;border:1px solid var(--line);border-radius:8px;padding:12px;margin:10px 0}input{margin-right:10px}small,legend{color:var(--muted)}button{font:inherit;color:var(--fg);background:var(--bg);border:1px solid var(--line);padding:12px;border-radius:8px;cursor:pointer}button:disabled{opacity:.5;cursor:default}pre{white-space:pre-wrap;overflow-wrap:anywhere;font-size:12px}@media(max-width:480px){main{padding:14px}section{padding:14px}}`;
await mkdir('dist', { recursive: true });
await writeFile('dist/index.html', `<!doctype html><html lang="ko"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><style>${css}</style><main id="app"><h1>SKIP</h1><p>현재 기록을 기다리고 있습니다.</p></main><script type="module">${js}</script></html>`);
